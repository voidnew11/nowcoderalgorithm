1、给定root,按层打印二叉树
last-当前行的最右节点；nlast-下一行的最右节点
若temp=last,换行；
让nlast一直跟踪节点

2、二叉树序列化与反序列化
二叉树->字符串（序列化）
字符串->二叉树（反序列化）
序列化方式：
先序遍历序列化；
中序遍历序列化；
后序遍历序列化；
按层序列化；
eg:先序遍历对二叉树进行序列化
1、假设序列化结果为str,初始时str为空；
2、先序遍历二叉树如果遇到空节点，则在str的末尾加上"#!";
3、如果遇到部位空的节点，假设节点值为3,则在str的末尾加上"3!".
str=12!3!#!#!#!;
